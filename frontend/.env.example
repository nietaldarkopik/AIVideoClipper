# URL of the backend API (backend/.env's APP_URL + "/api"). Defaults to
# http://127.0.0.1:8000/api when unset — only needed when the backend runs
# on a different host/port, e.g. a real domain on a fresh install.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/api
